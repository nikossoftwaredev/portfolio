const AboutSection = () => {
  return (
    <section
      id="about"
      className="w-full h-screen flex flex-col items-center justify-center"
    >
      About
    </section>
  );
};

export default AboutSection;
